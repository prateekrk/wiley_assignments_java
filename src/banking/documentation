!---------------------------------DOCUMENTATION---------------------------------!
*Class Account-> Integer account_id, double Balance.

*Class Operations-> Credit(), Debit():
      --Credit() get account_id from Account and add balance by credit amount
        And record it in transactions list
      --Debit() get account_id from Account and add balance by debit amount
        And record it in transactions list
*Class Transactions ->
       --String transaction_type,Double amount,Integer account_id
       -- Constructor() model for transaction

* Main class:
       --List of transactions
       --Accounts
       -- HashMap of Accounts
       --Operations

